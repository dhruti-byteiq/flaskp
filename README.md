Fullstack Dockerized Project – Node.js Frontend + Flask Backend

This project contains a simple Express (Node.js) frontend that sends form data to a Flask backend API.
Both services run inside Docker containers and are connected using Docker Compose.

🚀 Features

✔ Node.js + Express frontend with HTML form

✔ Flask backend that receives and processes submitted data

✔ Both services fully Dockerized

✔ Docker Compose connects both in the same network

✔ Clean folder structure (frontend + backend)

✔ .gitignore added for both services